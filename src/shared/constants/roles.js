
export const ROLES = {
  ADMIN: 'admin',
  MANAGER: 'manager',
  ORGANIZER: 'Organizer',
  USER: 'user',
};

export const ADMIN_ROLES = [ROLES.ADMIN, ROLES.MANAGER];

export const CONSUMER_ROLES = [ROLES.USER, ROLES.ORGANIZER];